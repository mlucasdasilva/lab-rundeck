version: "3.3"


services:

    servico1:
      image: sshd
      build:
        context: ./sshd
        args:
         - http_proxy=${HTTP_PROXY}
         - https_proxy=${HTTPS_PROXY}
         - HTTP_PROXY=${HTTP_PROXY}
         - HTTPS_PROXY=${HTTPS_PROXY}
         - no_proxy=${NO_PROXY}
         - NO_PROXY=${NO_PROXY}
      command: tail -f < /dev/null

    servico2:
      image: sshd
      command: tail -f < /dev/null

    servico3:
      image: sshd
      command: tail -f < /dev/null

    servico4:
      image: sshd
      command: tail -f < /dev/null

    servico5:
      image: sshd
      command: tail -f < /dev/null

    servico6:
      image: sshd
      command: tail -f < /dev/null

    servico7:
      image: sshd
      command: tail -f < /dev/null

    servico8:
      image: sshd
      command: tail -f < /dev/null

    servico9:
      image: sshd
      command: tail -f < /dev/null

